/**
 * Bcrypt hash for the shared TYLO Care temporary password (TyloCare@2026).
 * Safe to ship in the repo — override via BULK_USERS_DEFAULT_PASSWORD when rotating.
 */
export const TEAM_TEMP_PASSWORD_HASH =
  '$2a$12$Z3hjsh9R/eSB3uZxBEBz2eKJJbDdhO/ybwuhVYDZPQzznKMQoj47m';
